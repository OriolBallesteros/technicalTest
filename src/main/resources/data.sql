CREATE TABLE BRAND_ID (
  ID int NOT NULL,
  PRIMARY KEY (ID)
);
INSERT INTO BRAND_ID (ID) VALUES (1);

CREATE TABLE CURR(
  ID int NOT NULL,
  CURR varchar(255),
  PRIMARY KEY (ID)
);
INSERT INTO CURR (ID, CURR) VALUES (1, 'EUR');

CREATE TABLE PRICES (
  ID int NOT NULL,
  BRAND_ID int NOT NULL,
  START_DATE TIMESTAMP,
  END_DATE TIMESTAMP,
  PRICE_LIST int,
  PRODUCT_ID int,
  PRIORITY int,
  PRICE float,
  CURR varchar(255),
  PRIMARY KEY (ID),
  FOREIGN KEY (BRAND_ID) REFERENCES BRAND_ID(ID)
);
INSERT INTO PRICES (ID, BRAND_ID, START_DATE, END_DATE, PRICE_LIST, PRODUCT_ID, PRIORITY, PRICE, CURR)
  VALUES (1, (SELECT BRAND_ID.ID FROM BRAND_ID), '2020-06-14 00.00.00', '2020-12-31 23.59.59', 1, 35455, 0, 35.50, (SELECT CURR FROM CURR WHERE CURR.id = 1));
INSERT INTO PRICES (ID, BRAND_ID, START_DATE, END_DATE, PRICE_LIST, PRODUCT_ID, PRIORITY, PRICE, CURR)
  VALUES (2, (SELECT BRAND_ID.ID FROM BRAND_ID), '2020-06-14 15.00.00', '2020-06-14 18.30.00', 2, 35455, 1, 35.50, (SELECT CURR FROM CURR WHERE CURR.id = 1));
INSERT INTO PRICES (ID, BRAND_ID, START_DATE, END_DATE, PRICE_LIST, PRODUCT_ID, PRIORITY, PRICE, CURR)
  VALUES (3, (SELECT BRAND_ID.ID FROM BRAND_ID), '2020-06-15 00.00.00', '2020-06-15 11.00.00', 3, 35455, 1, 35.50, (SELECT CURR FROM CURR WHERE CURR.id = 1));
INSERT INTO PRICES (ID, BRAND_ID, START_DATE, END_DATE, PRICE_LIST, PRODUCT_ID, PRIORITY, PRICE, CURR)
  VALUES (4, (SELECT BRAND_ID.ID FROM BRAND_ID), '2020-06-15 16.00.00', '2020-12-31 23.59.59', 4, 35455, 1, 35.50, (SELECT CURR FROM CURR WHERE CURR.id = 1));











